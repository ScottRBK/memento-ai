[pytest]
pythonpath = .
markers =
    e2e: full end-to-end flow tests with real PostgreSQL
    integration: integration tests with stubbed dependencies
    asyncio: mark test as async
addopts = -m "not e2e"
asyncio_mode = auto
