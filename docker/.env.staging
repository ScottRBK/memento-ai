# ============================================
# Python Template - Staging Environment
# ============================================

# Application Info
SERVICE_NAME=Python Template
SERVICE_VERSION=v0.1.0
SERVICE_DESCRIPTION=Python FastAPI Template Service

# Server Configuration
HOST=0.0.0.0
SERVER_PORT=8020
LOG_LEVEL=INFO

# Docker Compose
COMPOSE_PROJECT_NAME=python_template
ENVIRONMENT=staging

# Docker Network Configuration
DOCKER_NETWORK_WEB=core_web
DOCKER_NETWORK_INTERNAL=core_internal
DOCKER_NETWORK_EXTERNAL=true

# Bind address for port mapping (127.0.0.1 for localhost-only)
BIND_ADDRESS=127.0.0.1

# Traefik Configuration
TRAEFIK_ENABLED=true
TRAEFIK_DOMAIN=python-template.scottrbk-kairos.duckdns.org
TRAEFIK_NETWORK=core_web
TRAEFIK_ROUTER_NAME=python-template-staging
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERT_RESOLVER=le
